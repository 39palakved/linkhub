{
  "name": "link-sharing-app-final",
  "version": "1.0.0",
  "description": "- rename `server/.env.sample` file to `.env` and add all the environment variable values",
  "main": "index.js",
  "scripts": {
    "start": "cd server && npm run start",
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
